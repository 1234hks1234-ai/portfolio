import lifelensImg from "../assets/lifelens.png";
import dataDriftMonitorImg from "../assets/data-drift-monitor.png";

// Maps a project's slug (from src/data/projects.ts) to its screenshot.
export const projectImages: Record<string, string> = {
  lifelens: lifelensImg,
  "data-drift-monitor": dataDriftMonitorImg,
};
